[package]
name = "stark"
version = "0.1.0"
edition = "2021"

[dependencies]
vm = { path = "../vm" }
trace = { path = "../trace" }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
